general {
  lock_cmd = pidof hyprlock || hyprlock -c /home/kriahna/.config/hypr/hyprlock/hyprlock.conf
  before_sleep_cmd = loginctl lock-session
  after_sleep_cmd = hyprctl dispatch dpms on
}

listener {
  timeout = 100
  on-timeout = hyprlock -c /home/kriahna/.config/hypr/hyprlock/hyprlock.conf
  on-resume = notify-send "Welcome back!"
}

listener {
  timeout = 100
  on-timeout = hyprctl dispatch dpms off
  on-resume = hyprctl dispatch dpms on
}
